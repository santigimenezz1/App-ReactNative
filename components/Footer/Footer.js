const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
    footer:{
        borderWidth:1,
        borderColor:"red",
        height:30,
        width:"100%", 
    }
})
export default styles